/target
_*.md
graphify-out
# Benchmark audio is downloaded on demand (deterministic) via
# tools/fetch-librispeech-samples.sh — never committed. samples/expected.txt
# (the labels/manifest) is the committed source of truth.
*.wav
